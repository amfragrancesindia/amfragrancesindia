import { Metadata } from 'next';
import CartPage from './page';

export const metadata: Metadata = {
  title: 'Shopping Cart | AMFRAGRANCESINDIA',
  description: 'View and manage your shopping cart.',
};

export default CartPage;
